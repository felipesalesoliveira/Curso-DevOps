# **CONFIGMAP**
- ConfigMap é usado para armazenar dados de configuração não sensíveis (ex: URLs, variáveis de ambiente, arquivos de configuração) que podem ser injetados nos Pods.
&nbsp;
# **SECRETS**
- Secret é usado para armazenar dados sensíveis, como senhas, tokens, certificados.